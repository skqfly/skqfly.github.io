---
title: "模型校准之前，先检查参数可辨识性"
pubDate: 2026-01-09
description: "以 Logistic 增长模型为例，说明拟合误差很小为何不等于参数已经被可靠识别。"
author: "skqfly"
category: "Mathematical Modeling"
image: { url: "/logo.svg", alt: "skqfly 标志" }
---

模型校准常被写成一个优化问题：给定观测，寻找使误差最小的参数。但“优化器找到了一个解”并不等于“数据足以确定这些参数”。如果参数不可辨识，不同参数组合可能给出几乎相同的预测。

## 从 Logistic 模型开始

考虑增长模型

$$
\frac{\mathrm dN}{\mathrm dt}
=rN\left(1-\frac{N}{K}\right),
$$

其中 $r$ 是内禀增长率，$K$ 是环境容量。给定观测 $(t_i,y_i)$，最小二乘目标为

$$
J(r,K)=\frac12\sum_{i=1}^{m}
\left[N(t_i;r,K)-y_i\right]^2.
$$

当所有观测都集中在增长早期、且 $N\ll K$ 时，模型近似为 $\dot N\approx rN$。这时数据对 $r$ 很敏感，却几乎看不到 $K$ 的作用。优化器仍可返回一个 $K$，但它通常由边界、初值或噪声决定。

## 用灵敏度矩阵观察问题

令参数向量 $\theta=(r,K)$，灵敏度矩阵写成

$$
S_{ij}
=\frac{\partial N(t_i;\theta)}{\partial\theta_j}.
$$

在独立同方差噪声假设下，Fisher 信息矩阵近似为

$$
F=\frac{1}{\sigma^2}S^\mathsf{T}S.
$$

若 $F$ 的最小特征值接近零，说明存在一个参数方向几乎不会改变预测。此时置信区间会很宽，且参数间常出现很强的相关性。

## 观测设计比更换优化器更有效

下面比较三种采样方案的典型表现：

| 采样方案 | 覆盖阶段 | $r$ 的识别 | $K$ 的识别 |
| --- | --- | --- | --- |
| 仅早期密集采样 | 指数增长段 | 好 | 很差 |
| 仅后期采样 | 平台附近 | 较差 | 好 |
| 早、中、后期均有采样 | 完整增长过程 | 好 | 好 |

如果 $K$ 不可辨识，增加同一时间段内的采样密度帮助有限；更有效的做法是延长观测窗口，让数据覆盖接近平台的阶段。这说明实验设计与参数估计不能完全分开。

## 校准结果应如何报告

除了最优参数和目标函数值，还应报告：

- 参数置信区间或剖面似然区间；
- 灵敏度矩阵或信息矩阵的条件数；
- 参数相关系数；
- 用未参与校准的数据进行的预测检验；
- 初值、参数边界与观测误差假设。

模型的用途通常是预测或解释，而不是得到一组看起来精确的小数。先确认参数是否真的被数据约束，再讨论优化精度，结论会可靠得多。
